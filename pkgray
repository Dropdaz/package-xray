#!/bin/bash
# Package-Xray - A Neovim-inspired CLI Package Manager
# Copyright (C) 2026 Ernesto Vives Femenia
# Licensed under GNU GPL v3.0

# 1. Detect the actual script directory (resolves symlinks)
SOURCE="${BASH_SOURCE[0]}"
while [ -h "$SOURCE" ]; do
  DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"
  SOURCE="$(readlink "$SOURCE")"
  [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE"
done
DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"

# 2. Launch Neovim with isolated environment and modern colors
# --clean: Prevents conflicts with user configs like LazyVim
# --cmd "set termguicolors": Enables 24-bit RGB colors
# --cmd "colorscheme habamax": Sets a clean, professional theme
exec nvim --clean -n \
    --cmd "set runtimepath+=$DIR" \
    --cmd "lua package.path = package.path .. ';$DIR/?.lua;$DIR/?/init.lua'" \
    --cmd "set termguicolors" \
    --cmd "colorscheme habamax" \
    -c "luafile $DIR/main.lua"